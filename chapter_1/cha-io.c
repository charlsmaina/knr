#include <stdio.h>

int main(void)
{
   // implementation using a for loop

   
   for(int c ;( c = getchar()) != EOF ; putchar(c));
  
     
    //implementation using a  while loop

    /*
     while(ch != EOF )
    {
         putchar(ch); // prints a single character from its ascii value
         ch = getchar();
    }
    */

   
    
}